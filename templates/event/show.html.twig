{% extends 'base.html.twig' %}

{% block title %}Events{% endblock %}

{% block body %}

    <div class="container">
        <div class="row g-3 justify-content-center">
            <div class="col-12 col-md-3">
                <div class="h2 retro-floral-bold">{{ event.title }}</div>
                <div class="retro-floral-bold">{{ event.startAt|date }} - {{ event.endAt|date }}</div>
            </div>
            <div class="col-12 col-md-8">

            </div>
        </div>
    </div>
{% endblock %}
